# Continuous Ink Mode — Observations Log

Running log of bugs, risks, compute-heavy code and cleanup opportunities spotted while executing
`2026-09-23-continuous-ink-mode.md`. **Flag only — do not fix anything here that is outside the task
being executed** unless the user says so. Related plan: `docs/superpowers/plans/2026-09-23-continuous-ink-mode.md`.

Entry format (one bullet each, newest last within a section):

```
- [Task N | role] KIND — path/File.kt:line — what is wrong or heavy, and why it matters — suggested fix — in-scope: yes/no
```

- `role`: `planner`, `implementer`, `spec-review`, `quality-review`, or `controller`.
- `KIND`: `BLOCKER` (ink issue that pauses the plan — see "Ink Blocker Protocol" in the plan; include exact evidence), `BUG` (wrong behavior), `RACE` (ordering/concurrency), `PERF` (compute, allocation, IO), `RISK` (fragile, could break later), `CLEANUP` (duplication, dead code, naming), `TEST-GAP` (untested behavior).
- Mark anything you did not verify by running or reading the exact lines as `(unverified)`.

## Pre-identified while planning (from reading the code; verify before acting)

- [planner] PERF — `ui/markup/PdfMarkupUi.kt:225-239` — `eraseAt` runs `activeStrokes.mapNotNull { distanceToViewStroke(...) }` for every stroke on every erase MOVE event, including each historical sample: O(strokes × points) plus a list allocation per sample, on the UI thread. — early-out with a per-stroke bounding box, reuse a buffer, or skip history samples closer than the hit radius — in-scope: no
- [planner] PERF — `data/PdfMarkupStore.kt:174-197` (`getMergedActiveStrokesByPage`) and `:208` (`loadAllTabletMarkup`) — parses every tablet markup JSON file in the job's tracker dir on each call. Before this change it ran on every page change; after it runs once per `markupChangeGeneration`. Large jobs with many tablets/pages still re-parse everything per markup change. — cache parsed files keyed by (name, lastModified, length) — in-scope: no
- [planner] PERF (unverified) — `ui/viewer/PdfMarkupObservation.kt` + `data/PdfMarkupStore.kt:151-172` — the `FileObserver` watches `CLOSE_WRITE`/`CREATE`/etc. in the tracker dir, so this tablet's own `savePageMarkup` likely bumps `markupChangeGeneration` and triggers a full reload after every stroke. — ignore events for own file, or debounce — in-scope: no
- [planner] RACE (blocker candidate, unverified) — `UnifiedReferenceViewer.kt` load effect + `PdfMarkupPageStates.replaceAll` — if a reload (fired by the observer, see above) reads the file before an in-flight async save lands, `replaceAll` overwrites the in-memory page with stale disk data and the newest stroke vanishes until the next reload. The same window existed before for the single page, but the effect now replaces every page. Verify in Task 4/5 by drawing two strokes quickly; if reproducible, treat as `BLOCKER` per the Ink Blocker Protocol. — merge instead of replace while saves are pending, or skip reload for own writes — in-scope: no
- [planner] RACE — `ui/viewer/UnifiedReferenceViewer.kt` (persist path, formerly `persistMarkupState`) — each save is a separate `scope.launch(Dispatchers.IO)`, so two quick strokes on the same page can reach `savePageMarkup` out of order and the older snapshot can overwrite the newer one. Pre-existing; the per-page persist lambda in Task 4 keeps the same behavior. — serialize saves per page with a single-consumer channel or `Mutex` — in-scope: no
- [planner] PERF — `data/PdfMarkupStore.kt:75-101` (`savePageMarkup`) — re-reads and rewrites the whole tablet file (all pages) for every single stroke or erase, and `loadTabletMarkup` logs via `AppLog.d` on every call. — in-memory copy of own file, write-behind — in-scope: no
- [plan-review] UPDATE — the PERF entry on own saves triggering a reload is **confirmed** by reading the code: `atomicWriteFile` (`data/AtomicFileWriter.kt:30-45`) renames `<tablet>.markup.json.tmp-N` onto `<tablet>.markup.json`, and the observer passes `MOVED_TO` for names ending `.json`. Both RACE entries above are now **in scope** and fixed by the plan: the reload guard in Task 1 and the single-writer `markupSaveDispatcher` in Task 4. The full-job reparse on every stroke (PERF) remains out of scope.
- [plan-review] RISK — `ui/markup/PdfMarkupUi.kt:197,258` + Compose UI 1.11.4 `PointerInteropFilter.android.kt` — the overlay returns `false` on a finger/palm `ACTION_DOWN` (finger drawing off), so the interop filter goes `NotDispatching` until every pointer lifts. A pen that lands while the palm is already down never reaches the overlay, in paged mode as well as continuous. — have the overlay accept `ACTION_DOWN` whenever `inputEnabled`, then ignore non-stylus pointers itself (this needs care: accepting the down makes the filter consume and would starve the parent scroll handler), or rely on OS palm rejection — in-scope: no
- [planner] CLEANUP — `ui/hardwoods/ClassicCutListTable.kt:92` and `:166` — a near-duplicate "does a finger lock the view" predicate (`allowFingerDrawing && activeTool != PAN_ZOOM`) that behaves differently from the new `shouldContinuousPaneOwnFingerGestures`. — consider one shared rule — in-scope: no
- [planner] CLEANUP — `ui/viewer/SheetViewerScreen.kt:409-520` — third copy of the local-strokes / deleted-ids / persist pattern (single-page). Could reuse `PdfMarkupPageStates`. — in-scope: no
- [planner] RISK — `ui/components/ContinuousReferencePdfPane.kt` (`ContinuousReferencePdfPaneTest`) — several tests assert on source text (`continuousPaneSource().contains(...)`), which breaks on refactors that keep behavior and passes on code that is wrong. Task 3's wiring guards follow that pattern. — behavior-level test around the handler — in-scope: no

## Task 1 — `PdfMarkupPageStates`

- [Task 1 | implementer] PERF — `ui/markup/PdfMarkupPageStates.kt:29` (`PdfMarkupPageSnapshot.visibleStrokes`) — computed eagerly from `strokes.filter { it.id !in deletedIds }` in every `copy()`, including the one `add()` makes on every stroke; on a page with N strokes this is an O(N) filter plus a new List allocation per stroke drawn, on the main thread. Verified by reading the code (matches the plan's implementation verbatim, so not changed here). — lazily compute `visibleStrokes`, or track deleted ids as a Set and diff incrementally — in-scope: no (plan text required verbatim)
- [Task 1 | implementer] CLEANUP — `ui/markup/PdfMarkupPageStates.kt:78` (`undoStack`) — entries are appended on every `add()` and never removed, even after the stroke they reference is erased (`erase()`/`undoLast()` don't prune the stack, only skip dead entries via `isVisible`). Over a long session with many strokes/erases across a large job, this list grows unboundedly for the life of the viewer. Verified by reading the code. — prune already-invisible entries out of `undoStack` opportunistically (e.g. in `commit()` or on `erase()`), or cap its size — in-scope: no (matches plan text verbatim; behavior-preserving for the 14 tests, not needed for this task)
- [Task 1 | spec-review] TEST-GAP — `ui/markup/PdfMarkupPageStates.kt:87-89` (`commit`'s `onSaved` guard, `seq > (lastSaved[key] ?: 0L)`) — no test exercises an out-of-order landing (older save's `onSaved` firing after a newer save for the same key already landed). Read the code: the `seq >` check is correct and prevents `lastSaved` regressing backward, so `beginReload`'s `unsavedAtStart` can't be fooled into treating an already-current page as stale — but this guard is exactly the mechanism the plan's amendment 1 relies on, and it has zero direct coverage. — add a test that calls two `commit()`s on the same key, lands the second `onSaved` before the first, then asserts a subsequent `beginReload()` does not mark the key unsaved — in-scope: no
- [Task 1 | spec-review] CLEANUP — `ui/markup/PdfMarkupPageStates.kt:551-566` (`replaceAll`) — verified by reading the code and the matching tests (guarded-reload trio, lines 316-356 of the plan / test file): `keep` correctly preserves pages with a pending unsaved edit at reload start (`key in guard.unsavedAtStart`) and pages mutated during the read (`lastMutation[key] > guard.seqAtStart`), and correctly lets a page go stale-checked once its save has landed before the reload started. `undoLast`/`hasUndo` also verified against all their tests (stack-first, own-strokes fallback, unknown-key and unknown-stroke no-ops). No defect found in this logic; noted here only because the task asked for explicit sign-off on it.

- [Task 1 | quality-review] BUG — ui/markup/PdfMarkupPageStates.kt:20-22,47 + plan Task 4 persist lambda — persist saves snapshot.visibleStrokes (merged, incl. other tablets' strokes) into this tablet's file; after the next reload ownStrokeIds includes other tablets' ids, so fallback undo can erase another tablet's stroke and deletion-wins propagates it everywhere. Same as today's undo (UnifiedReferenceViewer.kt:700-710), but the class comment claims otherwise. — persist only own visible strokes; fix comment; add test. Changes cross-tablet file contents, so user decides — in-scope: no (older than this change)
- [Task 1 | quality-review] RISK — ui/markup/PdfMarkupPageStates.kt:58-62,87-89 — if savePageMarkup throws, onSaved never fires and that page is kept stale by every later guarded reload; Task 4's scope.launch would also crash — controller: Task 4 persist now wraps the save in runCatching + AppLog.e and always calls onSaved — in-scope: yes (Task 4)
- [Task 1 | quality-review] RISK — ui/markup/PdfMarkupPageStates.kt:146-149 — replaceAll's guard defaults to null, so a future caller can silently bring back the stale-reload race — controller: guard made required, clear() added (Task 1 fix-up) — in-scope: yes
- [Task 1 | quality-review] PERF — ui/markup/PdfMarkupPageStates.kt:115-123 + Task 4 hasMarkupHistory — hasUndo read directly in composition scans the undo stack and recomposes the viewer on every page-map write — controller: Task 4 now wraps it in remember { derivedStateOf { } } — in-scope: yes (Task 4)
- [Task 1 | quality-review] PERF (unverified) — ui/markup/PdfMarkupPageStates.kt:71,80 — SnapshotStateMap tracks the whole map, so one add re-runs strokesFor on every visible continuous page; likely cheap (same list objects, children skip) — per-key state holders if profiling shows cost — in-scope: no
- [Task 1 | quality-review] CLEANUP — ui/markup/PdfMarkupPageStates.kt:11-13 — duplicates private PdfMarkupStore.normalizedPdfFilename (data/PdfMarkupStore.kt:33-34); drift would make pages silently lose strokes — shared normalizer on PdfMarkupPageKey in Models.kt — in-scope: no
- [Task 1 | quality-review] RACE (unverified, low) — ui/viewer/PdfMarkupObservation.kt:31-32 vs Task 4 persist — the MOVED_TO observer event can bump changeGeneration before onSaved runs; that reload keeps the page as "unsaved" and nothing re-reloads it, so remote strokes for that page stay hidden until the next change. Own strokes are not lost. — have onSaved request a reload if one ran while the save was pending — in-scope: no
- [Task 1 | quality-review] TEST-GAP — PdfMarkupPageStatesTest.kt — untested: undo-stack entry whose stroke vanishes in a reload; fallback undo persisting; erase counting as an edit for the guard; invalid keys in erase/undoLast — add cases — in-scope: no

## Task 2 — Gesture predicates

- [Task 2 | implementer] none — both new functions (`shouldContinuousPaneOwnFingerGestures`, `isStylusPointerType`) are pure, branch-free, and fully covered by the 4 new tests (truth table over `DrawingTool.values()`, both stylus pointer types, both non-stylus types). No new bugs, races, perf concerns, or duplication introduced by this task's own diff. The pre-existing `ClassicCutListTable.kt:92,166` near-duplicate predicate noted under "Pre-identified while planning" still applies but is unchanged by this task.
- [Task 2 | spec-review] TEST-GAP — `app/src/test/java/com/kkc/sheettracker/ui/components/ContinuousReferencePdfPaneTest.kt:622-638` — `fingerGestures_stayWithThePaneWhileInkIsOnAndOnlyTheStylusDraws` only asserts `selectedTool = PEN` and `HIGHLIGHTER` with `markupEnabled = true`; it never asserts `selectedTool = PAN_ZOOM` with `markupEnabled = true`, so that cell of the truth table (same branch as PEN/HIGHLIGHTER, but a distinct enum value) has no direct coverage — the first test's loop covers `PAN_ZOOM` only for `markupEnabled = false`. Verified by reading the test file and `DrawingTool` (`ui/markup/PdfMarkupSupport.kt:7-12`: `PAN_ZOOM, PEN, HIGHLIGHTER, ERASER`). — add `selectedTool = DrawingTool.PAN_ZOOM` to the second test's assertions — in-scope: no (plan's test list is verbatim; the branch is provably identical to the tested PEN/HIGHLIGHTER cases from the implementation, so this is low-value)
- [Task 2 | quality-review] CLEANUP (unverified — style-only, not a defect) — `app/src/main/java/com/kkc/sheettracker/ui/components/ContinuousReferencePdfPane.kt:50-51` — new `import androidx.compose.ui.input.pointer.PointerType` is placed after `androidx.compose.ui.input.pointer.pointerInput` (capital before lowercase would sort first in a strict ASCII alphabetical scheme); the plan only asked to add it "next to" the existing pointer imports, which this satisfies, and the surrounding file already has other out-of-order imports (`IntOffset`/`IntSize` at :57-58, `withFrameNanos`/`VelocityTracker`/`Job`/`DrawingTool` at :60-64) predating this change. — reorder if/when the file's imports are cleaned up generally — in-scope: no

## Task 3 — Pane gesture wiring

- [Task 3 | implementer] RISK — `app/src/main/java/com/kkc/sheettracker/ui/components/ContinuousReferencePdfPane.kt:1163` (was) — the early return `if (currentMarkupEnabled && isStylusPointerType(firstDown.type)) return@awaitEachGesture` originally sat before `flingJob?.cancel()` (per Task 3 Step 4a's literal ordering), so if a finger flick was still momentum-scrolling (`flingJob` active) when the stylus touched down with ink on, that fling was never cancelled and kept calling `listState.scroll()` while the pen drew on the now-moving page. Since `PdfMarkupOverlay`'s stroke coordinates are page-local but the pointer events it receives are screen-space, a stroke drawn while the page slides underneath it would record in the wrong place relative to the page content. — fixed by controller decision: moved the stylus early-return to sit after `flingJob?.cancel()` (still before `isInteracting = true`), so a pen touch now always cancels a running fling first, same as a finger touch; comment updated to state this; a source-order guard was added to `scrollZoomHandler_ignoresStylusPointers` in `ContinuousReferencePdfPaneTest.kt` asserting `cancelIndex < stylusCheckIndex < interactingIndex` — in-scope: yes (fixed in commit `c2bfae78`)
- [Task 3 | implementer] none further — the mid-gesture stylus-takeover check (`event.changes.any { it.pressed && isStylusPointerType(it.type) }`) adds no allocation (non-capturing lambda) and runs at most once per pointer event on an already-hot path that was doing comparable work before this change; no new per-frame cost of concern found.
- [Task 3 | spec-review] none — diff 1af7f5b7..c2bfae78 touches only the two planned files; every Step 3/4/5 edit is present and placed as the (amended) plan says; the extra fling-before-stylus ordering assertion in the test is the approved controller change.
- [Task 3 | quality-review] RISK — `app/src/main/java/com/kkc/sheettracker/ui/components/ContinuousReferencePdfPane.kt:1169-1237` — `isInteracting = true` is reset only on normal loop exit. If `gesturesEnabled` flips mid-gesture (now also when finger drawing is toggled or ERASER is selected, not only ink toggled), the `pointerInput` modifier is removed, its coroutine is cancelled, and `isInteracting` stays `true`. That leaves `settled` false (no sharp zoomed crop re-render) and makes the `scrollToPage` effect (:774) swallow external nav (scrollbar / page jumps) until a later finger gesture runs the handler again. With finger drawing on, the handler is absent, so it cannot recover on its own. Needs a second finger toggling the toolbar while the first scrolls, so it is rare, and it was already possible via the ink toggle. — wrap the `do { } while` loop in `try { ... } finally { isInteracting = false }` (keep the `if (stylusTookOver) return` after it) — in-scope: yes (cheap; the change added more triggers)
- [Task 3 | quality-review] TEST — `app/src/test/java/com/kkc/sheettracker/ui/components/ContinuousReferencePdfPaneTest.kt:128-131` — the `stylusTookOver` guard passes on the `var` declaration alone. Nothing checks that the loop-top check runs before `event.changes.forEach { it.consume() }`, which it must: if the pen's changes were consumed, the overlay's pointerInteropFilter would `stopDispatching` and send ACTION_CANCEL. Nothing checks that `if (stylusTookOver) return@awaitEachGesture` sits between `isInteracting = false` and `currentOnSingleTap?.invoke()` either. — add indexOf ordering assertions for both, using the same style as the existing cancel/stylus/interacting ordering check — in-scope: yes
- [Task 3 | quality-review] DOC — `app/src/main/java/com/kkc/sheettracker/ui/components/ContinuousReferencePdfPane.kt:1175-1178` (and plan amendment 3) — "won't see this pen until every pointer lifts" holds only when the palm and the pen hit the SAME page's overlay. Each page has its own pointerInteropFilter, and Compose sends a filter only the pointers that hit it. A pen on a different page than the palm gets a fresh ACTION_DOWN and draws. In that case the `break` is what keeps the stroke alive, because otherwise the handler's `consume()` would cancel it at the Final pass (PointerInteropFilter.android.kt:274-279). — reword the comment to "if the palm is on the same page" and note the break protects cross-page strokes; in Task 5 check 4, test the palm and pen on different pages — in-scope: yes (comment only) — UNVERIFIED on device
- [Task 3 | quality-review] DOC — `app/src/main/java/com/kkc/sheettracker/ui/components/ContinuousReferencePdfPane.kt:1150-1156` — the older "Do NOT cancel flingJob here ... flingJob will be cancelled via Job cancellation" comment contradicts `flingJob?.cancel()` 5 lines below, and the stylus path now relies on that explicit cancel, because a pen never starts a drag session that would pre-empt the fling. — trim the comment to say the cancel is deferred until after `awaitFirstDown` so the just-launched fling gets its first frame — in-scope: no (pre-existing)
- [Task 3 | quality-review] NIT — observations line above ("adds no allocation") — `List.any {}` is inline, so no lambda object is created, but it still allocates an Iterator for each event. The pre-existing `any { it.pressed }` / `forEach` calls in the same loop do the same, so the cost is negligible. `fastAny` (androidx.compose.ui.util) would avoid it — in-scope: no

## Task 4 — `UnifiedReferenceViewer` wiring

- [Task 4 | implementer] PERF — `ui/viewer/UnifiedReferenceViewer.kt` (`hasMarkupHistory`'s `derivedStateOf { markupPageStates.hasUndo(currentMarkupKey) }`) — `hasUndo`/`undoTarget` reads the whole `undoStack` (a `SnapshotStateList`) before checking any specific page, so `add()`/`erase()` on *any* page of the job invalidates this `derivedStateOf`'s recorded dependencies and forces it to recompute, not just writes to `currentMarkupKey`. `derivedStateOf` still skips the downstream recomposition when the boolean doesn't change, so this is bounded by undo-stack size and not a correctness issue, but it is a stronger "only when the answer flips" guarantee than "only when this page changes." Verified by reading `PdfMarkupPageStates.hasUndo`/`undoTarget` (`ui/markup/PdfMarkupPageStates.kt:112-123`). — key the read off `markupPageStates.strokesFor(currentMarkupKey)` first and only fall back to the stack scan when that's empty, or track undo-eligibility per key — in-scope: no
- [Task 4 | implementer] PERF (unverified) — `ui/viewer/UnifiedReferenceViewer.kt` continuous-mode `markupStrokesForPage` lambda calls `pdfMarkupPageKey(filename, page)`, which allocates a new `PdfMarkupPageKey` (and does a `.trim().lowercase()` on the filename) on every call. `ContinuousReferencePdfPane` calls this per visible page while scrolling, so this runs on the UI thread every frame per visible page. `currentMarkupKey` in the paged-mode branch has the same allocation, recomputed every recomposition of the whole composable (it isn't `remember`ed) rather than only when `resolvedPdfFilename`/`sourcePage` change. — remember `currentMarkupKey` against `(resolvedPdfFilename, sourcePage)`; have the pane cache/normalize the key once per page instead of per frame — in-scope: no
- [Task 4 | implementer] RISK — `ui/viewer/UnifiedReferenceViewer.kt:496` (`markupSaveDispatcher`) — this is a single file-level `Dispatchers.IO.limitedParallelism(1)` shared by every `UnifiedReferenceViewer` instance in the process, not one per job/store. In split-pane screens (`isSplitPaneActive`) two viewers open on different jobs now serialize their markup saves through the same one-at-a-time writer, so a burst of strokes in one pane can add latency to saves in the other, unrelated pane. This matches the plan's Step 1 text verbatim (required for this task), so not changed here. — scope the dispatcher per `pdfMarkupStore`/job instead of process-wide, e.g. a `limitedParallelism(1)` dispatcher remembered alongside `markupPageStates` — in-scope: no (matches plan text verbatim)
- [Task 4 | implementer] none further — self-review of the diff (Step 2-6 regions) against the plan's literal replacement text found no deviations; `git grep` for the removed names (`localMarkupStrokes`, `localDeletedIds`, `visibleMarkupStrokes`, `persistMarkupState`) returned nothing, compile and the three targeted test classes passed clean, and no ink/stroke-loss issue was found — no `BLOCKER`.
- [Task 4 | spec-review] none — `git diff 6e798927 f73ac201` touches only `UnifiedReferenceViewer.kt` (+64/-90). Every hunk is a Step 1-6 edit and matches the plan text exactly. The two removed imports (`mutableStateListOf`, `PdfInkStroke`) have no other uses in the file, `derivedStateOf` was already imported, and no `@OptIn` was needed. No unrelated feature code (zebra tints, navigator, stepper, haze) changed.
- [Task 4 | quality-review] VERIFIED — `ui/viewer/UnifiedReferenceViewer.kt:614-633` — save FIFO holds. Checked against the kotlinx-coroutines 1.9.0 sources in the gradle cache (1.10.2 is also present, no sources jar). `withContext` takes the SLOW PATH because the dispatcher changes: `startCoroutineCancellable` then `resumeCancellableWith` calls `LimitedDispatcher.dispatch` synchronously, inside the UNDISPATCHED body on the main thread. `dispatchInternal` then does `queue.addLast`, and the single `Worker` drains the queue with `removeFirstOrNull`. The fairness re-dispatch every 16 tasks does not reorder. So write order equals `persist` call order, as long as every call comes from the main thread, which `PdfMarkupPageStates` documents. If the scope is cancelled mid-save, the NonCancellable block still writes, but prompt cancellation on resume skips `onSaved()`. That is harmless because the states instance is being discarded. A cancelled reload (`withContext(Dispatchers.IO)`, :650) throws on resume, so its `replaceAll` (:656) never runs.
- [Task 4 | quality-review] RISK — `ui/viewer/UnifiedReferenceViewer.kt:642` — the load effect no longer runs when `sourcePage` or `resolvedPdfFilename` changes. Before this change, every page turn in paged mode re-read the disk. That gave a free recovery when the `FileObserver` missed an event: the re-arm gap in `PdfMarkupObservation.kt:27-36`, or `createTrackerChangeObserver` returning null when `mkdirs` fails. Remote strokes now show only after the next observer event. — also bump the reload on a cheap `trackerContentVersion` change, checked on page change or on a slow timer, or accept this and note it — in-scope: no (unverified on device)
- [Task 4 | quality-review] PERF — `ui/markup/PdfMarkupPageStates.kt:152-154` + `UnifiedReferenceViewer.kt:656` — every own save triggers a full reload (plan-review line above). That reload's `replaceAll` puts a new `PdfMarkupPageSnapshot` instance into every page. The map's value comparison is by identity, so every `strokesFor` reader is invalidated and gets a new (equal) `visibleStrokes` list. In continuous mode every visible page overlay recomposes and redraws once more after each stroke. In paged mode the whole viewer body recomposes once more. The earlier Task 1 PERF note assumed "same list objects", which does not hold after a reload. — in `replaceAll`, keep the existing instance when the new snapshot has the same stroke ids and `deletedIds`. Or skip the reload for own-file events — in-scope: no
- [Task 4 | quality-review] NIT — `ui/viewer/UnifiedReferenceViewer.kt:614` — when the store is null or the job is blank, `persist` returns without calling `onSaved`, so that page stays "unsaved" forever. This is unreachable in practice: the load effect clears the map and never reloads in that state. — call `onSaved()` in an `else` branch for symmetry, or add a comment — in-scope: no
- [Task 4 | quality-review] DOC — `docs/superpowers/specs/2026-09-23-continuous-ink-mode-design.md:62` — the spec says `hasUndo` is true for "any visible stroke on the centered page". The implementation (`PdfMarkupPageStates.kt:118`) falls back only to this tablet's own strokes. That is better: the old code enabled Undo for remote-only strokes and then did nothing. — update the spec wording — in-scope: no
- [Task 4 | quality-review] UX (spec-approved) — `ui/viewer/UnifiedReferenceViewer.kt:703` — in paged mode, Undo now pops the session stack across pages, so pressing Undo on page B can erase a stroke on page A that is not on screen, with no visible feedback. This matches spec lines 55 and 101. Flagging it for the on-device check in Task 5. — optional: a toast or snackbar naming the page, or jump to it — in-scope: no

## Task 5 — Verification / on-device

- [Task 5 | implementer] BUG (latent, fixed incidentally) — `ui/components/ContinuousReferencePdfPane.kt` old pageContent overlay + Compose `PointerInteropFilter.android.kt:325` (ui-android 1.11.4) — `pointerInteropFilter` builds its MotionEvent by subtracting only `localToRoot(Offset.Zero)` (a translation), and ignores any ancestor scale. The old per-page overlay sat inside the `sharedZoom` graphicsLayer, so at zoom > 1 the pen coordinates it received were in unscaled root-relative px. The strokes would have landed in the wrong place even if the crops had not hidden them. — the new outer-layer overlays have no scaled ancestor (the only transform is their own translation layer), so MotionEvent x/y equal overlay-local px — in-scope: yes (by construction) — UNVERIFIED on device: check that pen tip and ink line up at 1x, about 3x, and near max zoom
- [Task 5 | implementer] RISK — `ContinuousReferencePdfPane.kt` `ContinuousInkOverlayPlacer` — the overlays are placed from `host.localBoundingBoxOf(pageCoords)`. LazyList moves its items in its own placement pass, which runs after ours (it is deeper in the tree, and Compose relays out shallow nodes first). A read-only placement would lag one frame and stay wrong once scrolling stops. Each item root (the LazyColumn item Box, or the LazyRow wrapper) now calls `requestPlacement()` from `onPlaced`. That re-queues the overlay nodes directly through `invalidatePlacement()`, and the same `measureAndLayout` loop picks them up (`DepthSortedSet.popEach` loops until empty). — on the device, fling-scroll with ink visible, then check that the ink sits exactly on the page after the fling stops (1x and zoomed, vertical and horizontal) — in-scope: yes — UNVERIFIED on device
- [Task 5 | implementer] PERF — `ContinuousReferencePdfPane.kt` pane body + `pageContent` — the whole pane recomposes on every pan, zoom, and scroll frame. `overlayMotionFrame`, `zoomedIn`, and `maxEdgeOverscroll` are read in composition, and `pageContent` is a new lambda each time, so every composed list item recomposes on every frame as well (existing behavior). The ink layer is its own skippable composable, so it does not follow along, except on pinch frames where `zoom` changes. — possible future work: read the motion values in draw or placement only, and remember `pageContent` — in-scope: no
- [Task 5 | implementer] BEHAVIOR — `ContinuousReferencePdfPane.kt` `ContinuousInkOverlayLayer` — ink overlays now exist only for pages in `visiblePages` (at least partly on screen), not for every composed buffer page. The eraser radius (30 px) is now multiplied by `sharedZoom`, which keeps it matched to the visual stroke width. Before this change it was unscaled, in coordinates that were already wrong (see the first entry). — on the device, check that erasing at 3x feels proportionate — in-scope: yes
- [Task 5 | quality-review] STATUS: BLOCKED (ink) CRASH — `ui/components/ContinuousReferencePdfPane.kt:1500` + `:1624` — the overlay is measured with `Constraints.fixed(boxSize × sharedZoom)`, and CONTINUOUS_MAX_ZOOM is 20. Compose `Constraints` packs both dimensions into 31 bits (ui-unit 1.11.4 `Constraints.kt` `createConstraints`/`bitsNeedForSizeUnchecked`: a size of 8191 or more takes 15 bits, 32767 or more takes 16, 65535 or more takes 18). When both dimensions reach 32767 px, the call throws `IllegalArgumentException("Can't represent a width of … and height of …")`. Example: a Tab S8+ (SM-X800, 1752 px wide) in portrait with a full-width pane shows a portrait page at 1752×2267. At zoom 18.7 or higher that becomes more than 32767 × 42000, and the app crashes as soon as a page with ink mode on or saved strokes is composed. Older code never measured anything larger than the screen. — Do not measure the overlay larger than the screen. Size each overlay to the part of the page rect that falls inside the host box (at most the host size). Give PdfMarkupOverlay an explicit page-rect-in-view transform, for example an optional `pageRectInView: Rect` that `currentTransform()` and the draw block use in place of `computePdfPageTransform(viewSize)`. Then normalized ↔ view mapping stays exact, and the size never passes screen dimensions. Add a unit test that checks the overlay size stays bounded at zoom 20 for a 1752×2267 page. — in-scope: yes
- [Task 5 | quality-review] BEHAVIOR REGRESSION — `ui/markup/PdfMarkupUi.kt:184` + `:244` — the default `strokeWidthScale = viewportState.zoom.coerceAtLeast(1f)` also scales the eraser hit radius. Paged callers (`ReferencePdfPane.kt:799`, `SheetViewerScreen.kt:2720/2902`) pass their real zoom in `viewportState`, so in paged mode the eraser reach changes from a fixed 30 view px to 30 × zoom (150 px at 5x). A pass of the eraser at zoom now deletes nearby strokes the pen never touched. That breaks "default preserves other callers" (only stroke widths were preserved), and the new source test locks the regression in. — Keep `ERASER_HIT_RADIUS_PX` unscaled by default. Add a separate `eraserRadiusScale: Float = 1f`, or apply the scale only when it is passed explicitly, and have only the continuous pane pass `sharedZoom` for it. Change the `pdfMarkupOverlay_strokeWidthScaleDefaults…` test to assert the paged default. — in-scope: yes
- [Task 5 | quality-review] VERIFIED (no change needed) — `ContinuousReferencePdfPane.kt:1618-1633` hit testing — the placement node is measured at host size, but it is not a pointer node. In Compose 1.11.4 `NodeCoordinator.hitTest`, `head(PointerInput)` on the outer coordinator is null, so hitTestChild goes to the inner coordinator that owns the `pointerInteropFilter` node, and `isPointerInBounds` uses that coordinator's measured size (the page rect). `PointerInteropFilter` does not set `interceptOutOfBoundsChildEvents`. The minimum-touch-target expansion is 0 for page-sized nodes, and a stylus never gets it. So touches outside the page are not captured, and a parked overlay (`-(size+4096)`, clipped by the `clipToBounds` host) cannot be hit. The `onPlaced` → `invalidatePlacement` path runs in the same `measureAndLayout` (`requestRelayout` Idle branch + `popEach` loop) and cannot loop: the overlays are siblings of the list, so re-placing them never re-places list items. The `SideEffect` only writes on change and runs after apply, so there is no state write during composition. — none — in-scope: yes
- [Task 5 | quality-review] MINOR — `ContinuousReferencePdfPane.kt:1560` — `continuousInkOverlayOffset` rounds the live, fractional page bounds to whole px each frame, while the page (inside the scaled graphicsLayer) moves by fractions of a pixel. During a pan or pinch, ink can jitter up to about 0.5 px against the page, and size vs. offset rounding can differ by up to 1 px at the right and bottom edges. — Keep the integer placement for hit testing, but add the fractional remainder as the layer translation (`placeWithLayer(IntOffset(floor)) { translationX = frac.x; translationY = frac.y }`). The MotionEvent offset (`localToRoot`) includes layer translation, so input stays exact. — in-scope: no (cosmetic)
- [Task 5 | quality-review] MINOR — `ContinuousReferencePdfPane.kt:1658` — `isPageVisible` reads `visiblePages`, a `derivedStateOf` over layoutInfo, in composition. So an overlay for a page that has just scrolled in appears one frame late, and a page that has just scrolled out keeps its overlay at its last (stale) bounds for one frame, because its coordinates stay attached while it is not placed. — acceptable; if it shows up on device, also skip placement when the item was not placed this pass (for example, track a per-page placed-generation from the item `onPlaced`) — in-scope: no

- [Task 5 | implementer, fix round 2] CHECKED — `ContinuousReferencePdfPane.kt` crop `Canvas` (the "crop overlay is outside the graphicsLayer" block) — the lag that hit the ink should not hit the crops. The crops call `host.localBoundingBoxOf(pageCoordinates)` in the DRAW phase. By then this frame's layout is done (list items placed) and the layer parameters are updated, so they read the current page bounds. The ink's old placement-phase read ran before the list's placement pass. Their dst rects are integer-rounded (`IntOffset`/`IntSize`), so they can jitter by up to 0.5 px against the page base bitmap, but not by a whole frame. — no change — in-scope: no — UNVERIFIED on device: if crops also look like they trail during a pan, the same analytic transform could be used there
- [Task 5 | implementer, fix round 2] DEVIATION — `ui/markup/PdfMarkupUi.kt` `pageRectInView` — this is a provider `(() -> Rect?)?`, not the `Rect?` value the controller asked for. The rect is only known in the layout pass on each pan or scroll frame. As a value it would reach the overlay by recomposition one frame later, which is the lag being fixed. A plain holder, written in measure, feeds the provider. A `ContinuousInkRedrawNode` sits after the placement node, on the canvas's layer, and calls `invalidateDraw()` when the rect changes. — in-scope: yes
- [Task 5 | implementer, fix round 2] DEVIATION — `ContinuousReferencePdfPane.kt` `continuousInkOverlayFrame` — the sub-pixel part of the page position is carried in `pageRectInBox`, relative to the integer box, not in a `placeWithLayer { translationX }` fraction. The result is equally exact, and it needs no second transform in the pen path. `pointerInteropFilter` offsets the MotionEvent by `localToRoot` (`PointerInteropFilter.android.kt:325`), which includes the box's integer layer offset. Pen x/y are therefore box-local and map through the same rect the draw uses. — in-scope: yes
- [Task 5 | implementer, fix round 2] PERF — `ContinuousReferencePdfPane.kt` ink overlays — when the page moves inside its clipped box, the canvas is re-recorded on every pan or scroll frame. This happens whenever the page extends past the pane edge, which is the usual case. Committed strokes use a page-local `NormalizedStrokePathCache` and are drawn with a translate, so those frames rebuild no Paths; only a zoom change rebuilds them. The live stroke still rebuilds its Path on each draw, as before. — in-scope: yes
- [Task 5 | quality-review, round 2] RESOLVED — the crash entry above (7f1ec70d). Every Constraints path is now bounded: `continuousInkOverlayFrame` (`ContinuousReferencePdfPane.kt:1554`) clamps to [0, host] with floor/ceil, so `Constraints.fixed(frame.size)` is at most the host size, a missing frame gives `fixed(0, 0)`, and `layout(hostW, hostH)` is at most the parent max. The paged-eraser regression is also resolved: `eraserRadiusScale = 1f` by default. With `pageRectInView == null`, `currentTransform()` and the draw path produce the same transform as before (aspect/viewSize guards, same `computePdfPageTransform` args, same widths). The only addition is an unused `remember` slot. — none — in-scope: yes
- [Task 5 | quality-review, round 2] VERIFIED — `ContinuousReferencePdfPane.kt:1515` `continuousZoomLayerRectToScreen` matches the RenderNode layer matrix: translation is applied after the pivot scale, `p' = (p - c)·z + c + t`, with pivot = the default center of the zoom Box. The list is `fillMaxSize` at the Box's origin, so its size is the layer size. Axis choice and overscroll sign (`-overscroll·zoom`) are identical to the layer block. The holder is a plain field, not snapshot state. It is written in measure and read in draw and input, with a manual `invalidateDraw` through the redraw node on the inner (layered) coordinator. Nothing is written to snapshot state in measure or draw, and draw invalidation cannot re-trigger layout, so there is no loop. `invalidateMeasurement` from a list item's `onPlaced` reaches the Idle branch of `requestRemeasure`, and the same `popEach` loop processes it (`MeasureAndLayoutDelegate.kt:198-238`). Pen mapping with a clipped box is exact: MotionEvent coordinates are box-local via `localToRoot`, the transform uses zoom 1, pan 0, cx 0, so `x → (x - pageLeft)/pageWidth`. Clamping matches `normalizedPageToView`, and the cache uses `coerceIn(0, 1)` too. — none — in-scope: yes
- [Task 5 | quality-review, round 2] PERF (claim partly false) — `ui/markup/PdfMarkupUi.kt:454` — `NormalizedStrokePathCache` hits only when the float width and height are exactly equal to the last frame. `pageRectInBox` width/height come from `right - left` of mapped rects whose translation changes every frame, so float rounding varies. A float32 simulation (zoom 2.37, list scroll 1.37 px/frame plus pan) missed on 1681 of 3000 frames, so all committed paths are rebuilt on about half of scroll frames. — Compare with a tolerance (`abs(width - pageWidth) < 0.01f`, same for height), or key the cache on the page's unscaled box size × zoom. Add a unit test that a translated rect of the same size reuses the paths. — in-scope: yes (perf only, not a correctness bug)
- [Task 5 | quality-review, round 2] MINOR — `ui/markup/PdfMarkupUi.kt:206` + `:241` — `pageRectInView()` now returns null whenever no part of the page is visible. If that happens mid-stroke, for example after a programmatic `scrollToPage` while the pen is down, then `currentTransform()` is null and the filter returns false before the `when`. ACTION_UP never commits the stroke, and `isHandlingGesture`/`isDrawing`/`currentPoints` stay stale until the next DOWN. User gestures cannot scroll while a pen is down, so this is rare. — When the rect is null, still handle UP/CANCEL (reset the state, and commit using the last good transform), or keep the last non-null transform for the gesture's duration. — in-scope: no
- [Task 5 | quality-review, round 2] TESTS — `ContinuousReferencePdfPaneTest` — the mirror-math tests check hand-computed numbers, not the real layer matrix. Nothing tests `NormalizedStrokePathCache` (reuse, a new list rebuilds, a size change rebuilds). — Add a test that builds `androidx.compose.ui.graphics.Matrix` like RenderNode (translate(t) · translate(c) · scale · translate(-c)) and compares `map(rect)`, and add cache tests. — in-scope: yes

- [Task 5 | controller] RISK — ui/components/ContinuousReferencePdfPane.kt (ContinuousInkOverlayLayer visible-page set) — if a page drops out of the visible set while the pen is still down (only possible via programmatic scroll such as scrollToPage; fingers cannot scroll mid-pen), its overlay leaves composition, Compose cancels the gesture and the in-progress stroke is lost. 3d976164 only covers the page-rect-null case. — keep an overlay composed while it owns an active gesture — in-scope: no

## Task 6 — Commit

## Triage (filled by the controller at the end)

| Entry | Action | Owner |
|---|---|---|
