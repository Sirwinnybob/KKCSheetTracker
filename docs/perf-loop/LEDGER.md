# Perf-Loop Ledger

Persistent state across cold-context passes. **The agent reads this first and updates it last.**
Status values: `pending` · `in-progress` · `done`.

Branch for all loop work: `perf/loop-optimization`.

---

## Subsystem queue (work top-down)

Data/sync layer is first by owner decision — it feeds everything; slow dashboards grow from here.
UI order below is a starting guess; early data passes may re-rank it (note any change in the Pass Log).

### Phase 1 — Data / sync foundation (do first)

| # | Subsystem | Key files | Status |
|---|-----------|-----------|--------|
| D1 | Scan coordinators + change monitor | `data/ScanCoordinator.kt`, `HardwoodsScanCoordinator.kt`, `AssemblyScanCoordinator.kt`, `SpecialtyScanCoordinator.kt`, `TrackerChangeMonitor.kt`, `StaticCachePoller.kt` | done — findings-only (see note) |
| D2 | Progress stores + index caching | `data/ProgressStore.kt`, `HardwoodsProgressStore.kt`, `SpecialtyProgressStore.kt`, `SheetRipProgressStore.kt` | done |
| D3 | Unified metadata engine | `data/unified/FileBackedUnifiedMetadataEngine.kt` (+ `data/unified/`) | done |
| D4 | Repositories | `data/JobRepository.kt`, `HardwoodsRepository.kt`, `SpecialtyRepository.kt`, `SupplyRepository.kt`, `DeliveryScheduleRepository.kt`, `TimecardRepository.kt` | done |
| D5 | Sync + update | `sync/` (Syncthing*), `update/UpdateManager.kt` | done — findings-only (off-limits subsystem) |

### Phase 2 — UI (audit-ranked; refine as you learn)

| # | Subsystem | Key files | Status |
|---|-----------|-----------|--------|
| U1 | Dashboards | `ui/dashboard/` (`UnifiedModeDashboardScreen.kt`, `DashboardWidgetFactories.kt`), `ui/hardwoods/HardwoodsDashboardScreen.kt`, `ui/assembly/AssemblyDashboardScreen.kt`, `ui/supply/SupplyDashboardScreen.kt` | done |
| U2 | Viewers + markup | `ui/viewer/SheetViewerScreen.kt`, `UnifiedReferenceViewer.kt`, `ReferencePdfViewerScreen.kt`, `ui/components/ReferencePdfPane.kt`, `ui/markup/` | done — already optimized (no change) |
| U3 | Hardwoods workspace | `ui/hardwoods/HardwoodsWorkspaceScreen.kt`, `ClassicCutListTable.kt`, `HardwoodsJobsScreen.kt`, row helpers | done |
| U4 | Assembly + 3D viewer | `ui/assembly/`, `viewer3d/` | done |
| U5 | Specialty | `ui/specialty/` | done |
| U6 | Supply | `ui/supply/` | done — already optimized (no change) |
| U7 | Job detail / browser / search | `ui/detail/`, `ui/browser/`, `ui/search/` | done — findings-only |
| U8 | Navigation + scaffold | `navigation/NavGraph.kt`, `ui/components/AppScaffold.kt` | done — already optimized / off-limits (no change) |
| U9 | Shared components + calculator | `ui/components/` (tables, status, `CalculatorOverlay.kt`) | pending |
| U10 | Settings + timecard/hours/clock | `ui/settings/`, `ui/timecard/`, `ui/hours/`, `clock/` | pending |

---

## Pass log

Newest at top. One row per completed pass.

| Date | Subsystem | Commit | What changed | Verify |
|------|-----------|--------|--------------|--------|
| 2026-06-27 | U8 Navigation + scaffold | _(no change)_ | NavGraph.kt (3373L) is exemplary — every store/repository/coordinator/engine is constructed inside `remember(keys)` with correct keys (basePath, tabletId, view-only); nothing heavy in raw composition. AppScaffold.kt's relevant code is the frosted-glass haze shell (`Surface` + `hazeEffect`, lines 377-388), which is OFF-LIMITS per CLAUDE.md (must not refactor haze / `Surface(shadowElevation)` — reintroduces the shadow-bleed bug); no inline regex, no unremembered store/repo construction elsewhere. No low-risk change warranted. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (green baseline, no code changed) |
| 2026-06-27 | U7 Job detail / browser / search | _(this commit)_ | Findings-only. SearchScreen clean (filtering on `Dispatchers.Default`). JobDetailScreen acceptable — `getMaterialStatusCounts` runs per *visible* material, keyed, warm in-memory index, appState fast path; doc-presence/page-status loaded via `LaunchedEffect`+IO. JobBrowserScreen `jobUiStates` has the same all-jobs `getJobStatusCounts`-in-`remember` main-thread-I/O pattern as U4/U5, BUT the production `useAppState` path uses precomputed counts (no I/O) and it's the primary CNC list — converting to async adds a first-frame empty flicker that needs owner judgment. Parked to FINDINGS.md (with the exact produceState fix) rather than applied. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (green baseline, no code changed) |
| 2026-06-27 | U6 Supply | _(no change)_ | Already optimized. All three files (SupplyDashboardScreen, SupplyItemDetailScreen, SupplyItemEditScreen) wrap every repository read (`getItems`/`getItem`/`getComments`/`getCategories`) in `withContext(Dispatchers.IO)` inside `LaunchedEffect`/coroutines — no sync-I/O-in-remember (unlike U4/U5). Dashboard derivations (`tierGroups`, `categoryMap`) already memoized; no inline regex. The underlying `SupplyRepository.getItems()` N+1 was already fixed in D4. No low-risk change warranted. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (green baseline, no code changed) |
| 2026-06-27 | U5 Specialty | _(this commit)_ | Same main-thread-I/O fix as U4: `SpecialtyJobsScreen` ran `specialtyStateStore.deriveJobCards()` synchronously in `remember{}`, and specialty `deriveJobCards` resolves items per job (parses specialty_items/checklist/tablet_items + merges tracker progress) — heavy disk I/O on the UI thread. Converted `cards` to `produceState { value = withContext(Dispatchers.IO) { … } }` (sort preserved, same keys incl. sortByName). Other specialty files clean (no sync-I/O-in-remember, no inline regex; per-card `statusCounts` already remembered with keyed LazyColumns). Left untouched: the pre-existing uncommitted `SpecialtyJobDetailScreen.kt` working-tree change (not part of the loop). Minor item noted: `deliverySchedule = remember { fetchSchedule() }` (line ~103) is a single small JSON read on the main thread — low impact, left as-is. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (12s) |
| 2026-06-27 | U4 Assembly + 3D viewer | _(this commit)_ | Fixed main-thread I/O in `AssemblyJobsScreen`: `allCards`/`specialtyCards` ran `deriveJobCards()` synchronously in `remember{}` (composition = main thread), but that call builds CNC+hardwood progress indexes from tracker files on cache-miss (heavy I/O — the exact anti-pattern in the async-badge memory). Converted both to `produceState { value = withContext(Dispatchers.IO) { deriveJobCards() } }`, matching the dashboard's existing pattern; the screen already renders empty-then-populated as the scan completes, so the empty initial value is consistent (low-risk: "move existing I/O onto the Dispatchers.IO path"). Reviewed the rest: `AssemblyViewerScreen` already hoists its regexes + is well-memoized; `AssemblyDashboardScreen` handled in U1; 3D `ViewerServer.start()` retry-loop `Thread.sleep` is server-startup infra (off hot path, handles interruption) — left as-is. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (10s) |
| 2026-06-27 | U3 Hardwoods workspace | _(this commit)_ | Hoisted per-call `Regex(...)` constructions to top-level compiled `private val` constants, matching each file's own existing pattern: `HardwoodsRowHelpers` (`parseCabinetCounts` compiled its regex per row via `formatCabinetDisplay`; plus 2 fallback regexes) and `BoardStockUiSupport` (`boardStockKey` + `parseDimensionToken`, called per board-stock row). Same patterns, compiled once instead of per row. The big interactive files are already exemplary and untouched: `HardwoodsWorkspaceScreen` memoizes every derivation with correct keys + heavy I/O in `LaunchedEffect`+IO; `ClassicCutListTable` paginates with proper `remember`/keys; `HardwoodsJobsScreen` runs per-job derivation in `LaunchedEffect`+Dispatchers.IO. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (10s) |
| 2026-06-27 | U2 Viewers + markup | _(no change)_ | Surveyed all files (SheetViewerScreen 3317L, ReferencePdfPane 1178L, UnifiedReferenceViewer 982L, ReferencePdfViewerScreen, markup). Subsystem is already well-optimized: transforms use `graphicsLayer` (GPU, no recomposition); ALL PDF rendering runs on `Dispatchers.IO`; ReferencePdfPane uses a cached `PdfRenderEngine` + two-tier base/detail bitmaps + quantized-viewport re-render throttling + LRU thumbnail cache + `derivedStateOf`; UnifiedReferenceViewer uses proper `remember`/`derivedStateOf`/`LaunchedEffect`+IO; markup uses batched `MotionEvent` history for stylus. No low-risk wins found; the rendering/gesture pipeline is high-value/high-risk to touch. Per the loop's "already-optimized → mark done, don't force a change" guidance, no code modified. (Verified the one suspicious inline `buildPageToCabinets` at UnifiedReferenceViewer:257 is in a plain data-prep helper, not composable scope — not a recomposition smell.) | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (green baseline, no code changed) |
| 2026-06-27 | U1 Dashboards | aef35bf | Recomposition cleanup matching the existing CNC pattern. `CncDashboardContent` already wrapped its widget build in `remember(dashboard)`, but Hardwoods/Assembly/Specialty dashboards built their `DashboardWidgetModel` lists inline on every recomposition. Wrapped all three `build*DashboardWidgets` calls in `remember(...)` keyed on their inputs, and wrapped the inline `totalCounts` folds in `remember(summaries)` (in both `UnifiedModeDashboardScreen` Hardwoods content and `HardwoodsDashboardScreen`). build* factories verified pure. `AssemblyDashboardScreen`/`SupplyDashboardScreen` already well-remembered (state-backed lists, keyed LazyColumns, IO-backed loads); HardwoodsDashboardScreen heavy derivation already in produceState+Dispatchers.IO. No behavior change — same widgets, fewer per-recomposition allocations. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (15s) |
| 2026-06-27 | D5 Sync + update | _(this commit)_ | Findings-only — entire subsystem is off-limits per the loop's risk line (Syncthing health-check/watchdog = sync correctness; UpdateManager/DeviceOwnerUpdateFallback = outward-facing APK install + permission/device-policy flows). Network I/O is already on Dispatchers.IO; no hot paths, no clear bugs. Parked the one real issue: `checkForUpdates` parses APK archives + walks external storage **on the main thread** in onCreate AND onStart (every foreground) → start/resume jank; fix needs a threading split (IO scan + main-thread state/dialog apply) crossing into MainActivity lifecycle (medium). Also parked a minor dir-walk dedup. Both in FINDINGS.md. **Phase 1 (data/sync foundation) complete — D1–D5 all done.** Next: Phase 2 UI, starting U1 (dashboards). | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (no code changed; green baseline) |
| 2026-06-27 | D4 Repositories | f617a55 | Applied 1 low-risk N+1 fix: `SupplyRepository.getItems()` was calling `resolveStatus(id)` per item, each doing a full `statusDir.listFiles()` → O(items) directory listings (N+1 on the networked supply drive). Now lists the status dir once and reuses it across all items via a new `resolveStatusFrom(itemId, statusFiles)` helper; single-item `resolveStatus`/`getItem` unchanged. `DeliveryScheduleRepository` (single read+parse) and `TimecardRepository` (network on IO, shared OkHttp singleton, timeclock rules off-limits) are clean. `JobRepository`/`HardwoodsRepository`/`SpecialtyRepository` are thin engine delegators; their `getCncSnapshot(...).job` discard (`JobRepository:70`) and `getUpdatedJob` single-job `listJobsFromCacheOnly` scans are the same parked engine N+1 (FINDINGS.md updated to list these call sites). | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (11s) |
| 2026-06-27 | D3 Unified metadata engine | a7641ec | Applied 1 low-risk win: `resolveCabinetParts` (assembly cabinet-tap path) now resolves the cabinet index + CNC job + hardwood job from a single cached `loadStaticJobData` snapshot instead of three separate `getCabinetSheetIndex`/`getCncSnapshot`/`getHardwoodsSnapshot` calls — removes 2 redundant `cache_static.json` `lastModified()` stats and, crucially, the full `buildCncSearchIndex` allocation that `getCncSnapshot` did only for the result to be discarded. Behavior identical (same cached objects). Engine is heavily cache-coupled + format-coupled (StaticJobData is the on-disk `cache_static.json` Gson schema), so the bigger wins are parked: (a) D1 N+1 — confirmed `getJobInfo` exists but returns RAW unmerged jobInfo, needs a board-merging variant; (b) deep-parse path calls full `listJobs()` per job for lineupPosition (O(K·N)); (c) `getCncSnapshot` rebuilds search index per call, D4 `JobRepository:70` discards it. All in FINDINGS.md. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (13s) |
| 2026-06-27 | D2 Progress stores + index caching | 8ab9c74 | Applied 2 low-risk N+1 fixes. (1) `HardwoodsProgressStore.summarizeJob` now fetches `getRowProgressMap` once per job instead of once per document — was O(docs) full map copies per job summary, run per-job in dashboard `jobs.map { summarizeJob }` loops. (2) `SpecialtyProgressStore.loadResolvedItems` no longer loads the items twice on a cache miss — `loadMergedCompletionByItem` now receives the already-loaded list instead of re-running the full multi-file specialty parse. `SheetRipProgressStore` clean (atomic writes, small file). Parked the big one: CNC `ProgressStore` write path re-parses+re-serializes the whole tracker JSON per appended action (O(A·K) per sheet completion); needs the Hardwoods-style cache+async-persist pattern → FINDINGS.md (medium, write cadence observed by sync). | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (24s) |
| 2026-06-27 | D1 Scan coordinators + change monitor | 490ea0c | Findings-only pass. Profiled all 6 files + the engine methods they call. Subsystem is already tightly optimized (two-phase cache scan, coalescing, warmup, per-job invalidation, dedup). No low-risk code wins found; the real issues are above the line and parked: (1) N+1 — per-job `updateJobInState` re-scans every job via `listJobsFromCacheOnly()`; needs an additive engine accessor → D3. (2) per-job search-index rebuild + per-folder state emissions in the deep-load loop → couple with (1). (3) dead warmup branch in `TrackerChangeMonitor.flushPendingNow` (ambiguous intent). (4) `flushJob === this` self-clear never fires (benign). All 4 written to FINDINGS.md. | `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (33s, green baseline; no code changed) |

**Note on D1:** no code changes applied. Every actionable improvement was either engine-coupled (belongs to subsystem D3 — adding a single-job `getJobInfo` accessor) or had ambiguous intent (the warmup branch). Per the loop's "all medium/high-risk → mark done with a note and park proposals" rule, D1 is complete. The N+1 finding is the priority item to pick up when D3 runs.
